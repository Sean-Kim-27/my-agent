"""Behavior-level baseline scenarios for the Phase 0 eval suite.

These scenarios exercise the assembled agent (LLM + tools + memory + session)
rather than any single unit. They act as a regression net for later phases.
"""
