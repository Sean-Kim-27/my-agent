# 2026-06-12 작업 요약

- 요청: Ubuntu LTS / OCI 서버에서 Docker 없이 직접 구동하는 개인용 Codex OAuth 기반 Telegram 에이전트 설계 및 초기 구현.
- 참고: 현재 세션에서 Obsidian MCP 도구/리소스가 노출되지 않아 MCP를 통해 vault를 읽거나 쓸 수는 없었음. 대신 요청한 `codex/2026-06-12.md` 위치에 작업 요약을 남김.
- 공식 Codex 매뉴얼 확인: Codex CLI는 ChatGPT 로그인/OAuth 세션을 로컬에 캐시하며, headless 서버에서는 `codex login --device-auth` 또는 로컬 auth cache 복사가 권장됨. `codex exec`와 Codex SDK는 저장된 CLI 인증을 재사용할 수 있음.
- 구현: `python-telegram-bot` + `openai-codex` Python SDK 기반 봇 골격 생성. 앱 시작 시 `CODEX_WORKDIR`로 이동한 뒤 Codex thread를 생성하도록 정리.
- 주요 파일: `README.md`, `pyproject.toml`, `.env.example`, `src/personal_codex_agent/*`, `deploy/personal-codex-agent.service`.
- 운영 방식: 서버에서 `codex login --device-auth`로 OAuth 로그인 후, `.env`에 Telegram token과 허용 user id를 넣고 `systemd` 서비스로 실행.
- 문서 보강: 다른 에이전트가 혼동 없이 이어 개발할 수 있도록 `AGENTS.md`, `ARCHITECTURE.md`, `DEVELOPMENT.md`, `SECURITY.md`, `ROADMAP.md`를 추가하고 README에 문서 인덱스를 연결함.
- 남은 리스크: Obsidian MCP 도구/리소스가 현재 세션에 노출되지 않아 vault 내부 스킬/페르소나 참조와 MCP 기반 기록은 수행하지 못함.


## Phase 1 개발 시작 업데이트

- 요청: `~/my-agent`의 아키텍처 설계안을 확인하고 개발 시작.
- 구현:
  - `.gitignore` 추가로 `.env`, venv, cache, build 산출물, SQLite 로컬 상태 파일 제외.
  - `CODEX_TIMEOUT_SECONDS` 설정 추가 및 `CodexRuntime.run()`에 `asyncio.wait_for` 기반 timeout 적용.
  - `/status` Telegram 명령 추가: runtime started 여부, active session 수, model, sandbox, workdir, timeout 표시.
  - timeout 발생 시 일반 오류와 분리된 사용자 안내 메시지 반환.
  - `bot.py`가 테스트/문서 import 시 `openai_codex` 런타임 의존성 없이 import되도록 type-checking import로 변경.
  - 기본 테스트 4개 추가: config timeout parsing, runtime timeout/lock release, status 응답, help의 `/status` 안내.
  - README, ARCHITECTURE, ROADMAP, `.env.example` 갱신.
- 검증:
  - `PYTHONPATH=src pytest tests -q` → 4 passed.
  - `PYTHONPYCACHEPREFIX=/tmp/my-agent-pycache python3 -m compileall src` → 성공.
- 남은 리스크/다음 작업:
  - 실제 `openai_codex` 패키지는 현재 Python 환경에 설치되어 있지 않아 SDK 실구동은 아직 검증하지 못함.
  - 다음 우선순위는 heartbeat 메시지, status/logging 고도화, systemd hardening 검토.


## 패키지 설치 및 실구동 검증 업데이트

- 요청: 패키지 설치 후 실구동 검증까지 진행.
- 설치:
  - `.venv` 생성.
  - `pip install -e . pytest`로 runtime dependency 설치.
  - 테스트 실패 원인(`pytest-asyncio` 누락)을 확인하고 `pyproject.toml`에 `dev` optional dependency 추가.
  - `pip install -e '.[dev]'`로 `pytest-asyncio` 포함 설치.
- 추가 수정:
  - `README.md` 설치 명령을 `pip install -e .[dev]`로 갱신.
  - Telegram app 초기화 실패 시 cleanup 단계에서 `Updater/Application is not running` 보조 오류가 발생하는 문제를 TDD로 재현하고 `shutdown_application()` 헬퍼로 수정.
  - `tests/test_main.py` 추가.
- 검증:
  - `.venv` 기준 `pytest tests -q` → 7 passed.
  - `.venv` 기준 `PYTHONPYCACHEPREFIX=/tmp/my-agent-pycache-venv python -m compileall src` → 성공.
  - 설치 패키지 import 확인: `openai-codex 0.1.0b3`, `python-telegram-bot 22.7`, `personal-codex-agent 0.1.0`.
  - fake Telegram token으로 `personal-codex-agent` entrypoint 실행 → Telegram Bot API `getMe`까지 도달했고 예상대로 `InvalidToken`만 발생. cleanup 보조 오류는 제거됨.
  - `~/.codex/auth.json` 존재 확인 후 `CodexRuntime.start()`와 실제 `runtime.run()` smoke test 시도.
- 현재 blocker:
  - Codex SDK 실질 run은 `RuntimeError: Your access token could not be refreshed because your refresh token was already used. Please log out and sign in again.`로 실패.
  - 앱 코드/패키지 설치 문제가 아니라 서버 Codex OAuth 재로그인이 필요한 상태.

## 전체 로드맵 구현 업데이트

- 요청: 설계안대로 완성될 때까지 개발하고 모든 기능 테스트 통과 후 보고.
- 구현:
  - Phase 1: systemd hardening 옵션과 restart spam 제한 추가.
  - Phase 2: `SQLiteStorage` 추가, chat metadata/mode/message metadata 저장, `/reset` DB 상태 정리.
  - Phase 3: `/cancel`, `/new`, `/mode`, document upload metadata handoff 추가. Telegram Markdown parse mode를 쓰지 않는 plain text 응답 정책으로 Markdown escaping 리스크 제거.
  - Phase 4: `CODEX_CLI_FALLBACK` 설정과 SDK thread 생성 실패 시 `codex exec` fallback 추가.
  - Phase 5: `persona.py` registry 추가, `default/dev/research` mode별 instruction template 및 `/mode` 선택 연동.
  - 문서 갱신: `README.md`, `ARCHITECTURE.md`, `ROADMAP.md`, `.env.example`, `deploy/personal-codex-agent.service`.
- 테스트/검증:
  - `. .venv/bin/activate && pytest tests -q` → `25 passed in 0.92s`.
  - `PYTHONPYCACHEPREFIX=/tmp/my-agent-pycache-final python -m compileall src` → 전체 source compile 성공.
  - smoke: SQLite initialize/upsert, persona prompt, `CodexRuntime.status()` import/instantiate 확인 → `smoke-ok`.
- 참고:
  - `systemd-analyze verify deploy/personal-codex-agent.service`는 템플릿의 `/home/ubuntu/apps/my-agent/.venv/bin/python` 경로가 현재 개발 checkout(`/home/ubuntu/my-agent`)에는 없어 경로 경고가 발생함. 배포 경로에 설치되면 해소되는 템플릿 경고임.
  - Obsidian MCP 원격 vault 기록은 별도로 시도함.
